/**
 * Test for withBack.
 * Runs with mocha.
 */
'use strict'

const withBack = require('../lib/withBack')
const { ok, equal } = require('assert')

describe('with-back', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
