/**
 * Test for bindScope.
 * Runs with mocha.
 */
'use strict'

const bindScope = require('../lib/bindScope')
const { ok, equal } = require('assert')

describe('bindScope', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
