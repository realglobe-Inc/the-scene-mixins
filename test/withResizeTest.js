/**
 * Test for withResize.
 * Runs with mocha.
 */
'use strict'

const withResize = require('../lib/withResize')
const { ok, equal } = require('assert')

describe('with-resize', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
