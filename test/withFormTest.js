/**
 * Test for withForm.
 * Runs with mocha.
 */
'use strict'

const withForm = require('../lib/withForm')
const { ok, equal } = require('assert')

describe('with-form', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
