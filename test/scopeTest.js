/**
 * Test for scope.
 * Runs with mocha.
 */
'use strict'

const scope = require('../lib/scope')
const { ok, equal } = require('assert')

describe('scope', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
